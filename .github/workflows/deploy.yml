name: Deploy Review App

on:
  push:
    branches:
      - main # 👈 Triggers the build whenever you push to the main branch

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # Sets up Node without forcing the older Yarn v1 cache mechanism
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # Activates your specific local version of modern Yarn inside CI
      - name: Enable Corepack
        run: corepack enable

      # Uses modern Yarn's exact counterpart to --frozen-lockfile
      - name: Install Dependencies
        run: yarn install --immutable

      - name: Build Application
        run: yarn build

      - name: Upload Build Artifacts
        uses: actions/upload-pages-artifact@v3
        with:
          path: "./dist"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
