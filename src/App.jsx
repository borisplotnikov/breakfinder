import Layout from "./Layout";
import "./index.css"; // Global resets (margin/padding/height)
import "./App.css"; // Empty file (or safe to omit if deleted)

function App() {
  return <Layout />;
}

export default App;
